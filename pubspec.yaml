name: lupine_sdk
description: A dart package to store files on nostr using blossom.
version: 1.0.2
repository: https://github.com/nogringo/lupine-sdk

environment:
  sdk: ^3.7.2

dependencies:
  ndk: ^0.4.0
  encrypt: ^5.0.3
  hex: ^0.2.0
  crypto: ^3.0.6
  pointycastle: ^3.9.1
  path: ^1.9.1
  mime: ^2.0.0
  path_provider: ^2.1.5
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
